type Post{
    id:ID!,
    title:String!,
    description:String!
} 

type Query{
    posts: [Post]
} 

type Mutation{ 
    newPost(title:String, description:String) 
    updatePost(id, title:String)
}